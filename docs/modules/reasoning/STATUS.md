# Reasoning Status

## 当前状态

不作为独立顶层模块稳定实现。相关能力应收敛到 `rag` 的 analysis workflows。

## 依赖

- storage 已有 paper/chunk/object 数据。
- rag 能提供可靠上下文。
- LLM provider 可用。

## 暂不作为第一阶段目标

- 自动构建完整知识图谱。
- 大规模 claim consistency checking。
- 自动写论文相关长文档。
